DESARROLLO
==========

CONVERSACIÓN

--  Una conversación está definida por un valor del campo 'id'.
--  Campos a extraer: 'id', 'action', 'data', 'trigger'.
--  Registros a extraer:
    --  Por campo 'action':
        --  "ticket.create.language".
        --  "reply.text.create".        
        --  "classifications.add"
--  Filtro de criterios de clasificación de conversaciones:
    --  Por campos 'action' + 'data':
        -- "classifications.add" + "9", "10" ó "11" en 'data'. En el resto de conversaciones, poner un código 0, p.ej. 
--  Filtro de idioma de la conversación:
    --  Por campos 'action' + 'data':
        --  "ticket.create.language" + "Spanish"

BASE DE DATOS MYSQL
===================

-- db name: chatrhy

CREDENTIALS

--  MASTER USER: root/(micontraseña de equipo)

--  USER: pedro/21m01y20s20

REGENERACIÓN

    $ cd $CHAT_RHY

    $ mysql.server start
    $ mysql --local-infile -upedro -p21m01y20s20 chatrhy

    $ mysql> create database chatrhy;

    (creación usuario aplicación)
    $ grant all on chatrhy.* to 'pedro'@'localhost';

    $ mysql> use chatrhy;
    $ mysql> source src/sql_code/chatrhy_tables.sql;

    $ mysql> quit
    $ mysql.server stop

    